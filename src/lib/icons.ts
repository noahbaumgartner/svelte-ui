// Lucide icons, re-exported under `svelte-ui/icons` so consumers don't need
// their own @lucide/svelte install. Kept off the root entry because icon names
// (Link, Tag, Image, …) would collide with component names.
export * from '@lucide/svelte';
export type { LucideIcon, LucideProps } from '@lucide/svelte';
